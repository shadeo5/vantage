# Copy this file to `.env` and paste your key. `.env` is gitignored (never committed).
# Get a key at https://console.anthropic.com → API Keys. Only the Draft step needs it.
ANTHROPIC_API_KEY=sk-ant-your-key-here
