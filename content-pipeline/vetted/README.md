# Vetted per-city spot lists — the human-approved output of stage 2 (Curate -> Vet).
# Source of truth per the content ADR; consumed by generate.mjs (stage 3, draft).
# Produced by opening out/review.<city>.html, culling, and downloading vetted.<city>.json here.
