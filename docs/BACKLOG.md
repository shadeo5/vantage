# Vantage — Backlog (epics & stories)

**How we work (calibrated to stage):** epics/themes are mapped out now for tracking; **stories get detailed just-in-time** for the epic we're about to build (not the whole tree upfront), because the product is still pre-validation and requirements will shift. **Acceptance criteria + automated tests** are written for stable, regression-prone logic — not everything. Rigor scales up as the product validates or the team grows.

**Status:** ✅ done · ▶ in progress · 🎯 next · ⬜ planned · ◽ later

---

## DX · Agentic-development readiness  ✅ (2026-07-25)
Made the repo safe and fast for an AI agent (or a new contributor) to work in — closing the two gaps an agent leans on hardest: a way in, and a way to check its work. From an audit that scored the repo 5.8/10 (bones strong, front door thin, one drift landmine). **What shipped:**
- **Parity guard for the two-headed brain** (the P0). The scoring brain is hand-mirrored across `lib/nudge.ts` (app) + `supabase/functions/nudge/index.ts` (Deno push); nothing checked they agreed, so a weight changed in one place would silently make the push disagree with the app. New `__tests__/parity.test.ts` reads both sources and fails if the weights, go bar, `REF_FIT`, `LIGHT_SENSITIVITY`, `cloudFactor`, or `lightTiming` buckets drift. ⚠ banners added to both files.
- **A real agent front door.** `vantage/AGENTS.md` rewritten (commands, architecture map, the two-brain warning, conventions, doc links); new root `AGENTS.md` + `CLAUDE.md` that route to the right place; `docs/engineering/ADRS.md` index so the ADRs are discoverable.
- **One-command gate + CI.** New `npm run check` (typecheck + lint + test). `npm run typecheck` (`tsc --noEmit`) is now green **across source *and* tests** for the first time (fixed a missing `types: [jest, node]` in tsconfig — tests had never been type-checked). New `.github/workflows/ci.yml` runs `check` on every PR + push to main.
- **ESLint/Prettier** via `eslint-config-expo` (SDK 57 flat config). Green (0 errors). Deno function is ignored (its runtime); the web-DOM apostrophe rule is off (RN `<Text>` is fine, and the app is copy-heavy).
- **Pre-commit hook upgraded from reminder → gate** (`.claude/hooks/precommit-check.sh`): runs `npm run check` and blocks a failing commit, with a `--no-verify` / `[skip-check]` escape hatch and a graceful skip on a fresh clone.

**Follow-up (⬜, not blocking):** 18 lint **warnings** remain, mostly `react-hooks/refs` on the idiomatic `useRef(new Animated.Value()).current` animation pattern (shipped, tested, not bugs — the new React-Compiler-era rule is strict about it) plus one `set-state-in-effect` in `App.tsx`'s shown-log effect and a few `no-require-imports` in tests. Downgraded to warnings so they're visible guidance, not a blocker; revisit if we adopt the React Compiler.

---

## Fix · Hero anti-repeat  ✅ (2026-07-18) — ADR `docs/engineering/HERO_ANTI_REPEAT.md`
**User report:** Today kept headlining the same spot (the "Look At Them Look At Us" mural) every day. **Root cause:** a side-effect of CB7 — a flat-light *street* spot scores a **permanent ~0.96** (street is light-flexible × flat = usable all day), so it wins any daytime check; the day-shuffle (±0.10) can't overcome its lead and the shot-recency penalty only fires if you actually *shot* it, not if you're merely *shown* it. **Fix (a rule, not a weight):** an on-device **shown-log** (`lib/shownStorage.ts`) records the day's hero; `tonightNudge` now skips a spot headlined in the last `HERO_COOLDOWN_DAYS (2)` days **as long as another spot clears the go bar** (else an honest repeat). Only prior days count, so the pick is stable within a day (no feedback loop). Yesterday's hero drops into "best near you," so the headline cycles. `App.tsx` wired (load/save + a record effect). 148 tests (+12). Type-clean. **Deferred (deeper):** whether a flat all-day spot should *structurally* outrank time-specific windows — a scoring recalibration that overlaps CB6.

---

## DX · Build visibility  ✅ (2026-07-17)
Answers "is my phone on the newest build?" without asking. Two parts: (1) **auto-increment the preview build number** (`eas.json` preview `autoIncrement: true`) so every EAS preview build gets a unique versionCode — before this, every preview reported the same `1.0.0 (1)`, so nothing on-device distinguished builds. (2) **In-app build footer** — `lib/appVersion.ts` (`buildLabel()` reads `expo-application`'s `nativeApplicationVersion` + `nativeBuildVersion`) rendered at the bottom of the **Bag** screen as `Vantage · v1.0.0 (build N)`. Glance at the phone, compare the build number to the newest EAS build → know if you're current. Null-safe on web/Expo Go (`· dev`). Added `expo-application@57.0.2`. Typecheck clean, 136 tests pass.

---

## E0 · App shell & core UX  ✅ (built)
The nudge-first app, rebuilt from the Claude Design handoff v2.
- ✅ Today — gear banner, "your shoot tonight", inspiration hero + "why this pick?", ranked "best near you"
- ✅ Spot detail — hero, why, live light-ramp chart, "what to look for", gallery, getting there
- ✅ Plan — this-week slate, day groups, Light/Activity/Event tags, "I'm going"
- ✅ Bag — gear onboarding (cameras, lens/style chips)
- ✅ Lock — notification moment → tap to enter
- ✅ Bottom tab nav (Today / Plan / Bag)
- ◽ Saved collection screen (hearts persist somewhere) — deferred

---

## E1 · Content  🎯 NEXT — the biggest open workstream
The app currently runs on 5 hand-curated Atlanta spots + Unsplash placeholders. This epic answers: where does real content come from, and in what voice?

**ADR: `docs/engineering/CONTENT_PIPELINE.html`** — the content-at-scale architecture (Nashville/Chicago/Blue Ridge). 5-stage assembly line (Curate → **Vet 👤** → Draft → Image → Serve) with the human gate on the cheap candidate list before any spend. **Both/and data model:** git-committed per-city JSON = source of truth (PR-reviewed), Postgres = serving layer (an importer loads approved JSON), Supabase Storage = images. Kills the current duplication (spots hand-synced in `lib/spots.ts` **and** `supabase/functions/nudge/index.ts`). Schema = existing `Spot` type + `city_id` + `image_url`, no PostGIS yet. Decisions — **all 3 locked**: **D1** curation OSM-only for city #1 (reuse `scout.mjs`; human vet fills gaps; escalate to OSM+LLM-scout only if vetting is mostly *adding* spots OSM missed). **D2** draft one-shot by default (escalate a spot to a research-agent pass only when vet-stage copy is vague/unverifiable). **D3** imagery via **OpenAI GPT Image 1.5** (~$0.07/img; per-city reference image for cohesion; WebP-compress <300 KB; escalate to FLUX/Imagen only if consistency is weak). ADR status: Accepted — build next.
- 🎯 **C1 — Spot sourcing strategy.** Decide how spots are generated/curated at scale (LLM-assisted from OSM + local knowledge? editorial? community?). *AC: a documented, repeatable way to produce a new city's spot list with the fields the UI needs (name, why, look-for, getting-there, coords, window type).* **→ designed in the ADR; curation (stage 1) is the real open build (OSM backbone + optional LLM-scout, prototype at `archive/poc/scout.mjs`).**
  - ⏸️ **Content-drafting pipeline BUILT, running DEFERRED** (`content-pipeline/generate.mjs`): places (name+coords+genre) → Claude (`claude-opus-4-8`, structured output = the `Spot` schema, voice guide baked in) → reviewable `out/atlanta.json`. Validated the *quality* by hand (8 ATL spots, user approved). **Not worth the API spend pre-validation** — the app runs fine on the 5 curated placeholder spots, and bulk generation only pays off once we need volume (launch, or a 2nd city). Code is committed; has never been run (no `out/atlanta.json`, no key used). WHEN REVISITED: run it (needs ANTHROPIC_API_KEY), review output, wire JSON into `vantage/lib/spots.ts`. Still separate: the *curation* step (which places make the list — OSM + local knowledge).
- ✅ **C2 — Imagery (interim).** AI-generated **stylized illustrations** as honest placeholders (art, not fake photos of real places). Direction + copy-paste prompts at `docs/design/ILLUSTRATION_STYLE.md`. DONE: user generated all 5, wired into `vantage/assets/spots/*.png` + `lib/spots.ts` (local `require()` map replacing the Unsplash URLs, `img()` now returns a bundled module); the "what people shoot here" gallery was REMOVED (can't honestly show illustrations as real user photos). Typecheck + 32 tests pass. FOLLOW-UPS (deferred): images are ~3 MB each / ~15 MB total — resize/compress before ship; real place photography (user's own / Wikimedia) is still the eventual replacement. *AC met: hero images load per spot; no fake stock misrepresenting a place.*
- ✅ **C3 — Voice & tone guide.** 1-page guide at `docs/design/VOICE.md` (extracted from the pipeline's baked-in voice + the gold-standard spot copy). Existing app copy audited against it: mostly on-voice; fixed 2 real drifts — invented hours ("last entry 9 PM" / "parking after 7 PM" → generalized, per the honesty rule) + Plan-screen jargon tags (Light/Activity/Event-driven → "The light" / "The crowd" / "Happening"). Open polish (logged, not done): Plan fit-label register, generic "Add your gear", hardcoded "M" avatar. Imagery-misrepresentation flag handed to C2.
- ✅ **C4 — Content generation pipeline.** Tooling to draft per-spot copy at scale, human-reviewed. **→ ADR stages 3–5, all built + run.** Draft (`generate.mjs`) wired to the vetted list; auto-imagery (stage 4, `build-images.mjs` → GPT Image 1.5 → WebP → Storage); Serve layer (importer → Postgres `spots`/`cities` + Storage; app city-pack read + nudge Edge Function both point at the table). Nashville (13) + expanded Atlanta (11 new + 5 core) drafted, imaged, and live.
- ✅ **C5 — Data model / serve (from the ADR).** `cities` + `spots` tables in Supabase (`genres text[]`, `image_url`, public-read RLS on `published`); `build-seed.mjs` importer loads git-approved per-city JSON → `seed.sql`; app reads the city pack (`lib/cityPack.ts`) with the bundled-Atlanta B3 fallback; nudge fn reads the same table. **Done across Serve slices 1–3.** *AC met: adding a published city needs no app release and no hand-synced copies — the app + nudge both read the `spots` table.*
- Note: mixed-genre content (street + cityscape + nature) is confirmed good — validated as "top areas to shoot today."
- **First slice (per ADR build order):** Curate → Vet for one city (Nashville), OSM-only → run the built draft pipeline on the vetted list → eyeball quality. Then stand up the tables + importer and retire the duplication.
  - ▶ **Curate BUILT** (`content-pipeline/curate.mjs`, `npm run curate -- <city>`): Overpass (reused from `archive/poc/scout.mjs`, no key) → `classify()` buckets each POI (Cityscape/Street/Street art/Architecture/Nature/Landscape/**Landmark**) + maps to a valid app Genre → genre-profiled scoring/dedup → `out/candidates.<city>.json` + a self-contained **`out/review.<city>.html`** vet surface (checkbox reject, inline name/genre edit, category chip, **0–100 score** rel to #1, map/street/osm links, "download vetted JSON"). City briefs for nashville/chicago/blue-ridge. Splits `tourism=artwork` by `artwork_type` → murals = Street art, statues = **Landmark** (genre Details, low weight → demoted). **Run live on Nashville**: 159 POIs → top 25; surfaced the Pedestrian Bridge (#1), Farmer's Market, real murals (RONE, #educationisnotacrime). REMAINING GAP (the D1 escalation signal): misses vibe-streets (Broadway, Gulch) — what the human vet adds and the LLM-scout would later close. `out/` is gitignored; vetted list lands in `content-pipeline/vetted/`.
  - ✅ **Vet DONE (Nashville)** — 13 of 25 kept → `content-pipeline/vetted/nashville.json` (committed source of truth). User edited several genres (downtown pedestrian → Architecture). Each entry carries **both `genre` (primary, what `generate.mjs` reads) and `genres[]` (multi-genre array)** from a manual reasoning pass = the **genres[] prototype** (confident on known spots; web-checked the thin-knowledge Mixtape mural — the D2 escalation, live). Validates multi-genre is worth automating. **ADR schema flipped `spots.genre` → `genres text[]`** (design of record; no live migration — the `spots` table isn't built yet, only `profiles`/`nudge_log` exist). Remaining automation: fold `genres[]` into the draft schema so `generate.mjs` infers it per spot.
  - ✅ **Draft WIRED + RUN (Nashville)** — `generate.mjs` reads `vetted/<city>.json` (`npm run generate -- <city>`, `--limit=N` for a taste test), passes multi-genre hints, carries `genre` + `genres[]` through. Ran all 13 for **$0.17** (~$0.013/spot; opus-4-8). Copy is on-voice + grounded; the multi-genre payoff shows (WhatLiftsYou = Street/Portraits "stand-in-the-middle portrait"; Seigenthaler `look` spans Street/Architecture/Landscape). **Promoted to committed `content-pipeline/content/nashville.json`** = the reviewed source of truth the importer loads (`out/` stays gitignored scratch).
  - ▶ **Atlanta EXPANSION (curate)** — added an `atlanta` city brief that **excludes the 5 live spots** (don't re-curate what ships) + a **per-bucket diversity cap** (MAX_PER_BUCKET=6) because Atlanta's hundreds of mapped murals were flooding the list. Re-run: 357 POIs → diverse top 25 (Street/Street art/Architecture/Nature 6 each; Fairlie-Poplar #1). `out/review.atlanta.html` vetted → **11 of 25 kept → `vetted/atlanta.json`** (user name-edits: Underground Atlanta ← Alabama St SW, Georgia Aquarium ← Pemberton Place; multi-genre arrays added; 3 genre flags noted for later — Botanical Garden/Liberty Plaza/Georgia Aquarium primaries are debatable). ✅ **DRAFTED ($0.14) → committed `content/atlanta.json`** (11 new spots). Key now loads from gitignored `content-pipeline/.env` (`.env.example` committed). Nice: the model's display `type` self-corrected the 3 flagged genres (Botanical Garden/Aquarium → Nature) while the vetted `genre` stayed the user's pick — revisit `genres[0]` later if desired. **Note:** do NOT regenerate the 5 hand-authored ATL spots (gold-standard copy); expansion adds *new* spots only — they rejoin at Serve.
  - ▶ **SERVE slice 1 (backend) — schema + seed BUILT.** `vantage/supabase/schema.sql`: `cities` + `spots` tables (`genres text[]`, `image_url`, public-read RLS on `published`). `content-pipeline/build-seed.mjs` (`npm run seed`) → `vantage/supabase/seed.sql`: **29 spots = Nashville 13 + Atlanta 16** (5 hand-core in `content/atlanta-core.json`, copy preserved + 11 new). Dollar-quoted (safe apostrophes), idempotent, leaves `image_url` null for the Image slice. ✅ **DB seeded** (user ran schema + seed in Supabase — `cities` 2, `spots` 29). ✅ **Nudge fn repointed** (`supabase/functions/nudge/index.ts`): reads Atlanta published spots from the `spots` table (16, up from 5 hardcoded) → **duplication retired**; dropped the fake `distance` from the copy (no GPS = don't fake it). ✅ **DEPLOYED + TESTED** (`?force=1` → `{considered:2, sent:2, skipped:0}`, no "no spots" error = DB read confirmed; pushes sent). **SERVE SLICE 1 COMPLETE** — nightly nudge reads from Postgres; duplication retired. ✅ done.
  - ▶ **SERVE slice 2 (Images) — script BUILT + tested.** `content-pipeline/build-images.mjs` (`npm run images -- <city>`, `--limit`/`--dry`): GPT Image 1.5 (D3) + `ILLUSTRATION_STYLE.md` master block + per-light palette → WebP <300 KB (sharp) → `out/images/<city>/`. Test image (Nashville bridge, $0.07, 245 KB) **matches the existing hand-made style** — text-only prompt, no reference image needed. Deps: `openai`, `sharp`; `OPENAI_API_KEY` in `.env`. ✅ **all 24 generated** (13 Nashville + 11 Atlanta, ~$1.70, all <300 KB WebP in `out/images/<city>/`). Quality verified across all 3 light windows (blue/golden/flat palettes each distinct + on-style; the WhatLiftsYou wings mural even shows silhouettes posing *in* the wings). ✅ reviewed + **uploaded to the public `spots` Storage bucket** (verified HTTP 200 image/webp). `build-image-urls.mjs` (`npm run image-urls`) → `vantage/supabase/image_urls.sql` (24 `UPDATE`s → deterministic public URLs; 5 core spots stay null = bundled fallback). ✅ **SERVE SLICE 2 DONE** — `image_urls.sql` run; DB verified (29 spots: 24 with Storage URLs, 5 core null = bundled fallback). All content + imagery now live in Postgres/Storage.
  - ✅ **SERVE slice 3 (app reads the DB) — DONE.** `vantage/lib/cityPack.ts` — `fetchCityPack("atlanta")` reads published Atlanta spots from Postgres, maps rows → the app `Spot` shape (`genres text[]` → primary `genre` + `genres[]`, `image_url` → remote source), and falls back to the bundled 5-core pack on any error/empty (never leaves the screen blank). `lib/spots.ts` is now **types + bundled fallback only**: `FALLBACK_SPOTS` (the 5 hand-core), `spotImageSource(spot)` (remote `{uri}` when the spot carries a Storage URL, else the bundled illustration — the 5 core keep `image_url` null → bundled art), `findSpot(pack, id)`. The nudge brain takes the pack as a param now (`tonightNudge(spots, …)` + new `rankSpots(spots, …)` powering "Best near you"). `App.tsx` seeds state with the bundled core for an instant first paint, then swaps to the DB pack on launch (stale-while-revalidate; = the ADR's B3 bundled-Atlanta fallback). **The duplication is fully retired** — both the app and the nudge fn now read the same `spots` table; `lib/spots.ts` no longer holds the runtime list. **Verified in-browser:** Today shows **16 Atlanta spots** (was 5), tonight's hero is a *new* expansion spot ("Look At Them Look At Us") with its **Storage-served illustration** rendering, detail view loads remote image + DB copy + live light chart, Plan resolves the core ids against the loaded pack (bundled art). No `[cityPack]` fallback warning = live DB read confirmed. Small honest wins folded in: the hero's genre chip shows the real `spot.type` (was hardcoded "Street life"); the distance chip hides for GPS-less DB spots. 81 tests pass (was 73). **Scope:** the app is pinned to downtown Atlanta (device GPS is P2), so it reads the **Atlanta** pack — Nashville is published in the DB and rides along the moment a city-switch/GPS lands, no app release needed. ✅ **SERVE COMPLETE** (slices 1+2+3): content + imagery live in Postgres/Storage, and the app + nudge both read it. 🎯 **E1 content-at-scale is now end-to-end.**

---

## E2 · Personalization & Gear  ⬜
Make "your kit" real and load-bearing (it's currently a static "35mm").
- ✅ **G1 — Persist the gear profile.** Selected lenses persist on-device via AsyncStorage (`lib/gearStorage.ts`) — hydrated on launch, saved on change, survives reload. Verified: 46 tests (incl. save/load round-trip + corrupt-data guards) + clean headless web bundle. (Camera still fixed X100VI, so only lenses persist for now; backend sync is later.)
- ✅ **G2 — Gear → subject matching.** Telephoto→distance/sports; fast prime→low-light street; wide→architecture; macro→details. Drives the "why … your kit" line and per-spot "fits/grab a tele". CATALOG + MATCHING ENGINE BUILT: `vantage/lib/gear.ts` (19 cameras, 24 lenses w/ real specs; `equivalentFocal`/`genresForLens`/`genresForKit`, crop-factor aware) + `__tests__/gear.test.ts`. WIRED INTO THE APP (2026-07-12): Bag screen is catalog-backed with a live "Your kit shoots" row (`lib/gearProfile.ts`); Today hero + Lock copy name the lens that actually fits tonight's spot — annotate-only + honest (a tele won't be called a street lens; falls back to the street-capable body). Spots gained a `genre` field for the fit check. DONE: camera picker — pick from all 19 catalog bodies on the Bag screen → the kit recomputes per body (e.g. a 35mm reads differently full-frame vs APS-C) → persists across reload. Verified end-to-end in-browser. ✅ G1 persistence (camera + lenses) · ✅ Plan "fits" labels gear-aware (`fitLabel`, honest — never claims a lens that doesn't cover the spot) · ✅ Today/Lock copy gear-aware. Known simplifications: phones filed as "1-inch" (crop math still lands ~24mm); `genresForKit` assumes any lens fits any body (over-generous — future: match lenses to compatible mounts). **ADR: `docs/engineering/GEAR_MATCHING.html`** (rules + the open filter/re-rank/annotate decision).
- ⬜ **G3 — Gear banner re-prompt logic.** Dismiss = 30-day floor; re-ask event-based (after "I'm going" / on 2nd–3rd return), not a blind timer.

---

## E3 · The nudge & notifications  ✅ (live, end-to-end)
Turn the in-app hero into an actual proactive nudge — the north star.
- ✅ **N1 — "Is today great?" threshold.** A quality bar that decides whether to nudge at all (inspire, don't nag). `lib/nudge.ts` — `tonightNudge(now, camera, lenses)` scores every spot (light-timing × gear-fit; activity = weight-0 placeholder for events), picks the best, returns go/no-go + confidence + Why-signals. Pure/testable (13 tests). WIRED into the app + verified in-browser: the brain now picks the Today hero, sets the confidence badge ("TONIGHT'S PICK · MEDIUM"), and generates the "Why this pick?" signals (Light timing / Activity placeholder / gear fit). Honest: with no weather/events yet, "light" scores WHEN the good light is, not cloud cover. NEXT (N3/N2): richer nudge-copy generation, then real push delivery (needs backend E4 + dev build).
- ✅ **N2 — Push delivery** — LIVE. Deployed Supabase Edge Function (`supabase/functions/nudge/index.ts`, a Deno port of the brain) reads profiles + sends via Expo Push; scheduled nightly by `pg_cron` (`vantage-nightly-nudge`, `0 22 * * *` ≈ 6 pm Atlanta). Real push verified end-to-end onto the Pixel — manual curl, deployed-function invoke, and the scheduled job all confirmed. Needed the dev build (E5/P1) for the token.
- ✅ **N3 — Nudge copy generation** — personal, non-spammy, gear-aware. `lib/nudgeCopy.ts` — `nudgeCopy(verdict, lens, now)` composes a fresh push title+body from confidence-tiered phrasing pools, weaving in the spot, the fitting lens, the window + time, and distance. Variety is DETERMINISTIC (day-of-year seed → fresh per day, stable within a day, testable). Tone tracks confidence (high = "Tonight's the night", medium = "worth a look", no-go = honest "quiet one"). Wired into the Lock screen (the push mock), verified in-browser. 4 tests. Voice per `docs/design/VOICE.md`.
- ✅ **N4 — Shooting check-in / journal.** "Did you get out?" → doubles as the engagement/return metric *and* a shooting log. `lib/journal.ts` (+ `components/CheckInCard.tsx`): "I'm going" records a persisted Commitment; on RETURN in a later session the Today screen asks "Did you make it to [spot]?" → [I shot it / Not this time] → resolves into a persisted JournalEntry. A "Your shoots — you've been out N times" stat surfaces the return metric. Closes the loop: nudge → shoot → return → check-in. All on-device (AsyncStorage); opt-in photo-EXIF match is the later strengthening. Verified end-to-end in-browser. 3 tests. (Also fixed: "Best near you" now excludes the dynamic hero, not the old hardcoded HERO_ID.)

---

## E4 · Backend & data  ▶ (foundation LIVE)
**Design:** `docs/engineering/PUSH_ARCHITECTURE.html` (ADR) — Supabase cron → the ported nudge brain → Expo Push. Blocked on the user creating a free Supabase project (URL + anon key) + a free Expo account; then I scaffold DB + edge function + app-side push registration.
- ✅ **B1 — Supabase** — DB (profiles + push token + gear + nudge_log) + the scheduled edge function running `nudge.ts`/`nudgeCopy.ts` server-side. Project live + creds validated (`yqulquuzhkiwbucpqdcm`). DONE: app Supabase client (`lib/supabase.ts`, anon key via gitignored `.env`, RN url-polyfill + AsyncStorage per Supabase docs); schema at `vantage/supabase/schema.sql` (profiles + nudge_log, RLS scoped to `auth.uid()`, anonymous-auth model). Dashboard steps DONE (schema run + anonymous sign-ins enabled); full path validated by hand (anon sign-in → RLS insert → read → delete). **App↔cloud sync LIVE** (`lib/sync.ts`): app anonymously signs in on launch and upserts the gear profile to `profiles` — verified in-browser (console `[sync] profile saved`, row visible in Table Editor). Dev build + push token: `expo-notifications`/`expo-device`/`expo-constants` installed; push registration wired + verified (`lib/push.ts` → token → `savePushToken` → `profiles.push_token`, no-op on web); `app.json` (android package `com.shadeo5.vantage` + notifications plugin) + `eas.json` (preview = internal-distribution APK) set. DONE: `eas login` (shadeo5) + `eas init` (projectId `487f03db…`); Firebase project `vantage-57f96` + `google-services.json` wired via `android.googleServicesFile` (committed — EAS builds from git). **ALL DONE ✅** — Pixel preview-APK build installed + push token saved to `profiles.push_token`; Firebase project `vantage-57f96` + FCM service-account key uploaded to EAS; Edge Function deployed + scheduled via `pg_cron` (`vantage-nightly-nudge`). Push verified end-to-end. **GOTCHA (cost us a rebuild):** EAS builds from git, so a gitignored `.env` never reaches the cloud build — Supabase creds must be set as **EAS env vars** (`eas env:create`, preview env), not just `.env`.
- ⬜ **B2 — Precompute + serve** — scheduled job generates districts per city into Postgres; app reads a ready list (near-instant loads; no phone hits Overpass). *(Layer 1 of the "loads instant" plan.)*
- ⬜ **B3 — On-device cache** — stale-while-revalidate; instant relaunch even before a backend. *(Layer 2 — cheap early win.)*
- ▶ **B4 — Real events integration** — ESPN (sports, proven) + tentpole editorial (+ Ticketmaster later), geo-filtered. (Prototyped in `archive/poc`.) **GROUNDWORK (2026-07-16):** the golden-set sourcing strategy is written — `docs/engineering/EVENT_DATA_SOURCES.md` (multi-source thesis; per-city must-have *acceptance test*; Ticketmaster spine + CVB *trust tier* + ArcGIS permit feeds; normalization layer). First **curated editorial catalog** built + date-verified: `content-pipeline/content/atlanta-events.json` — 12 marquee, annually-recurring, photographable events, Opportunity-shaped (`kind:'event'` + window/magnitude/venueSpotId/kitAngles); hand-curated from the research, NOT auto-ingested. Dates checked against official sources: **Pride** (Oct 10–11 '26), **L5P Halloween** (Oct 17–18 '26), **Shaky Knees** (relocated → Piedmont Park, Sept 18–20 '26) confirmed → `high`; **Music Midtown dropped** (dormant since 2023); 5 spring-2027 events kept as `needs-date-verify` (recurrence re-verified, official dates not yet published). **✅ APP SLICE SHIPPED (2026-07-25) — events wired into the nudge.** `lib/events.ts` (Opportunity type + bundled Atlanta catalog + live/upcoming time-gates + the Spot adapter that lets a live event ride the existing hero/brief/detail via the ADR's inherit/override seam) + `components/ComingUp.tsx`. **Two surfaces:** (1) **takeover** — a LIVE event outscores spots (`scoreEvent` = light×gear + magnitude scarcity boost, capped) and headlines with a "Happening" Why-signal, demoting a co-located venue spot (eclipse rule, `bestNearYou` multi-exclude); verified in-browser via a simulated Sep-5 Dragon Con (hero = the event, adapted spot, no crash). (2) **"Coming up"** — forward shelf on Today (next ~60d, tap-to-expand why/look/getting) so events deliver value in the lead-up, not just day-of; verified live (Dragon Con "In 6 weeks", Shaky Knees "In 8 weeks"). Events city-gated to Atlanta; detail/commit resolution merges adapted live events so tapping an event hero resolves. 167 tests (+19), app source type-clean. **Freshness rule written** (`EVENT_DATA_SOURCES.md` — re-verify a season ~6wks out; `windowConfidence` = the signal). **Still ⬜ (slice 2):** push parity (port events into the nudge Edge Function + move the catalog to a Postgres `events` table, retiring the bundle like spots), the rain/`conditions` read into the push, and a live source (Ticketmaster spine). Event imagery is a default-illustration fallback for venue-less events (future).
- ▶ **B5 — Supply analysis** — run the engine across many days/times to measure how many worth-it options a day has (decides single-hero vs. list). Partial until N1 exists. **First product answer shipped:** Today is now hero + a **capped (max 4), quality-gated** "best near you" — not the full ranked pack. `bestNearYou()` in `lib/nudge.ts` keeps only spots that clear the go bar on their own (so a quiet night honestly shows fewer/none and the section hides), sorted, sliced to `MAX_NEAR_YOU=4`. Rationale (product/UX): too many options dilute the pick, imply a precision light×gear doesn't have past the top few, and turn an inspiration surface into a directory. The label dropped the misleading total ("16 tonight" → "N more"). The single tunable constant is the hook for later personalization and for the actual supply-analysis pass that would tune the number. 4 tests. **Deliberately NOT added: a "see all" on Today** — exhaustive browsing belongs on a future Explore/Map surface, keeping Today curated. **VARIETY shipped** (so Today doesn't headline the same spot every night): `varietyRank()` reshuffles the ORDER (hero + list) with two bounded, honest signals — a per-day rotation (`daySeed`+hash, stable within a day; since many spots tie at the same hour this alone rotates the pick day-to-day) and a **recency penalty** for a spot you just SHOT (reads the on-device journal, decays to zero over `RECENCY_DAYS=4`). Both only reorder — the quality gate + the "is tonight worth it?" verdict still read the REAL base score, so variety never promotes a clearly worse spot (`RECENCY_W=0.18 > VARIETY_W=0.10`). `tonightNudge`/`bestNearYou` take an optional `{ journal }`. 4 tests (rotation across days, recency demotes a just-shot spot, never overrides a clearly-better one, stable within a day). Verified in-browser: injected "shot Jackson today" → hero stepped aside Jackson → Atlanta Botanical Garden and Jackson dropped out of the top set. Remaining personalization runway: saved-affinity + shooting-intent.
- ⬜ **B6 — 🐛 Clock-skew → anonymous JWT rejected ("issued at future") → DB reads fall back to bundled.** *(logged 2026-07-16)* **Symptom:** on some cold loads the console warns `[cityPack] load failed, using bundled fallback: JWT issued at future` (from `lib/cityPack.ts:93`), and the app shows only the **5 bundled Atlanta spots** instead of the live 16. **Cause:** the app's anonymous session token (`lib/sync.ts` anon sign-in) is stamped with this machine's clock (the dev environment is set to a future date); when that's ahead of Supabase's servers beyond their leeway, the server rejects the token as future-dated. **Repro:** intermittent; seen at session start (~12:47pm), *self-resolved* by later in the same session (`[sync] profile saved` succeeds, live DB reads — so auth recovers once the session settles). **Impact:** low + self-healing — `cityPack.ts` already degrades gracefully (bundled 5-core, never blank); the only cost is bundled-not-live data on the affected load. **This is an environment artifact, not an app bug** — no code change needed. **Fix levers if it ever goes persistent:** (1) correct the dev machine / sandbox clock (root cause); (2) optionally make the fallback quieter / retry auth once on this specific error. **Tell-tale:** seeing 5 Atlanta spots instead of 16 = this, not a data problem.

---

## E5 · Platform & ship  ▶
- ✅ **P1 — Dev build** (EAS, Android). Preview-APK profile (`eas.json`, internal distribution) built + installed on the Pixel; push token registered (`lib/push.ts`) → saved to the profile. Firebase FCM configured (`google-services.json` + service-account key on EAS). iOS build later (needs Apple Developer, $99/yr). *(Also fixed expo-doctor — `@types/jest` dedupe.)*
- ▶ **P2 — Real location** (device GPS) instead of hardcoded downtown Atlanta. **City switcher DONE** (manual half): the Today header has a `⌖ <City> ▾` pill (`components/CitySwitcher.tsx`) that opens a modal list of the published cities (`fetchCities()` reads the `cities` table) and switches the loaded pack; the choice persists (`lib/cityStorage.ts`, AsyncStorage) and wins over the Atlanta default. Verified in-browser: Atlanta (16) ↔ Nashville (13) swap live — hero, "best near you", and per-spot light all recompute (Nashville golden hour reads later, matching its western longitude). REMAINING (the GPS half): on first launch, default to the nearest published city from device position (`expo-location` → haversine to `cities.center_lat/lon`), Atlanta fallback, manual pick still overrides. Deferred per Desha's call (ship the switcher first; GPS needs the dep + a Pixel rebuild to test the permission prompt).
- ✅ **P3 — Weather (Open-Meteo)** → weather-aware light. `lib/weather.ts`: `fetchCloudCover(lat,lon)` pulls hourly `cloud_cover` from Open-Meteo (free, no key, `timezone=auto`, 2-day forecast) → a `CloudCover.at(date)` lookup (0..1); best-effort, null on any failure = astronomical-only fallback (never blank). The honest model — clouds aren't uniformly bad: `cloudFactor(cloud, type)` tempers **golden/blue** hard (→~0.4 at overcast, the warm low light is what cloud blocks) but leaves **flat daylight** alone / mildly lifts it (overcast = soft even street/portrait light); monotonic decay, never *rewards* cloud on golden since one cloud-% can't tell thin-high from thick-low. Wired into (a) the detail **light chart** (`hourlyLight` takes an optional `CloudCover`, bars show the light you'll ACTUALLY get; "When the light works" header now reads e.g. "6:34–8:12 PM · mostly cloudy") and (b) the **nudge brain** (`scoreSpot`'s light term × weather; the "Light" Why-signal names the sky). `App.tsx` fetches once per loaded city (stale-while-revalidate), threads `cloud` into `tonightNudge`/`bestNearYou`/`SpotDetail`. 20 new tests (101 total). Live API verified (Atlanta, `America/New_York`, keys aligned). ✅ **FOLLOW-UP DONE (2026-07-22): weather-honest push.** Ported `cloudFactor` + a minimal Open-Meteo fetch (`cloud_cover` only) into the Deno nudge function so the *push* scores with the real sky, matching the app — closes the app↔push disagreement on cloudy evenings. **TZ gotcha handled:** the app keys cloud by *local* hour (phone clock = city tz); the Edge Function runs in UTC, so it queries Open-Meteo with `timezone=UTC` and keys on `getUTC*`. Fetched once per run (spots[0] city-proxy, same simplification as the app's Today), sampled at each spot's window start (mirror of `scoreSpot`); null forecast → astronomical-only fallback. Response gained a `weather:"live"|"unavailable"` diagnostic. **Verified** with a Node harness (live Open-Meteo Atlanta parses; an overcast evening flips the pick golden-landscape→flat-street) + a live `?force=1` invoke (`{sent:2, weather:"live"}`). Deployed (function v5). **Device verify pending** (chart caption + sky read on the Pixel).
- ◽ **P4 — Accounts / auth** (anonymous → real, once cross-device sync matters).
- ◽ **P5 — App Store + Play submission.**

---

## E6 · Quality & maintainability  ▶
- ✅ **Q1 — Dead code removed** (old OSM areas/events + old components).
- ✅ **Q2 — Unit tests on core logic** (`lib/light.ts`, `lib/spots.ts`) incl. a regression guard for the suncalc degrees/radians gotcha. `npm test`.
- ⬜ **Q3 — Metrics instrumentation** — engagement (nudge response + return), "I'm going" taps. Truth-checked against real shooting.
- ⬜ **Q4 — Error / loading / empty states** across screens (network images, data fetches).
- ◽ **Q5 — Component / e2e tests** once flows stabilize.
- ◽ **Q6 — Accessibility pass** (labels, contrast, touch targets). *(See UX on-device pass in E7.)*

---

## E7 · Experience & polish  ▶ (from the Jul-13 UX review — `docs/UX_REVIEW.html`)
Now that the app is on a real Pixel, the job is closing "impressive demo → feels finished." The review logged **2 P0 · 10 P1 · 7 P2**. Below is the **authoritative ordered work-list** — one row per review finding (its `#ID`), in the order to build. This is what the `/loop` run walks top-to-bottom.

**Status (2026-07-14):** the full checklist was worked top-to-bottom on branch `e7-ux-review` — **all rows ✅ in code**. What remains is device-only verification (row 15 flags): haptics feel, notification appearance, safe-area insets, thumb reach, real-light contrast, permission timing — do these after an `eas build` on the Pixel.

**Ordered checklist (each row = one review finding; build in this order):**
| # | ID | Sev | Item | Status |
|---|----|-----|------|--------|
| 1 | #B1 | P0 | **Bag tab ≠ onboarding** — strip "SETUP · 2 OF 3 / Continue / Skip"; make the Bag *tab* a calm gear manager ("Your bag", changes auto-save, single "Done" or nothing). | ✅ eyebrow→"YOUR GEAR · SAVES AS YOU GO", title→"Your bag", removed Continue/Skip (swipe nav + auto-save make them moot); dropped `onContinue`. |
| 2 | #P1 | P0 | **Plan week hardcoded / wrong day labels** — "TONIGHT · SAT" on a Monday. Near-term: generate day labels from the real date. Longer-term: drive the week from the nudge brain. | ✅ (near-term) day labels derived from real date; event cards anchored to their real weekday (`when: {dow}` → Sunday market on a Sunday, Fri art walk on a Friday); sorted by date; "Tonight/Tomorrow · Dow". Brain-driven week still longer-term. |
| 3 | #T1 | P1 | **Gear banner persists after gear set** (ties **G3**) — only show when gear is empty; else drop or a quiet dismissible "Tuned to your a7 IV · edit." | ✅ banner gated on `lenses.length === 0` — drops out in the default state (ships with a lens), reappears only if you clear all lenses (where "Add your gear" is accurate). Copy reword → #B2. |
| 4 | #T3 | P1 | **Hide "Activity — coming soon"** in "Why this pick?" until events exist — two strong reasons beat two + an IOU. | ✅ dropped the activity signal from `scoreSpot` (weight was already 0, score unchanged); "Why this pick?" now shows Light + Your kit. Type key kept for when events land; test updated. |
| 5 | #T2 | P1 | **"MEDIUM" confidence reads as "meh"** — flourish for HIGH only ("TONIGHT'S PICK ✦"), nothing extra on medium, genuinely weak night → honest "quiet night." | ✅ badge: HIGH→"TONIGHT'S PICK ✦", MEDIUM→plain "TONIGHT'S PICK" (no raw grade), !go→"A QUIET NIGHT" (dimmed dot, no pulse) with honest hero name + lede instead of "alive tonight / grab your lens and go". |
| 6 | #D1 | P1 | **Heart/save doesn't persist** — wire to local+cloud (patterns exist) or remove until a Saved surface exists. *(decision: persist vs remove — default persist local, flag cloud.)* | ✅ **default call: persist locally.** New `lib/savedStorage.ts` (AsyncStorage, same best-effort pattern as gear) + 5 tests; `saved` hydrates on launch and persists on every toggle. FOLLOW-UPS (flagged): cloud sync (mirror to `profiles` like gear), and a Saved surface to browse hearts. |
| 7 | #P2 | P1 | **Plan's four full-width gold "I'm going" buttons flatten hierarchy** — demote per-card action to secondary (outline/compact pill); reserve solid gold for the one primary CTA. | ✅ per-card CTA is now a compact, left-aligned **outline pill** (gold border, faint tint) instead of a full-bleed solid-gold bar; "going" = green outline. Solid gold now belongs only to the Today hero. |
| 8 | #B2 | P1 | **No real first-run path to gear** — decide the model. Default (recommend): no wizard — Today works with sensible defaults, Bag is always gear-management, banner (when unset) is a gentle "personalize your picks →". *(pairs with #B1.)* | ✅ **default call: no wizard.** Model now: Today works on sensible defaults, Bag is always gear-management (#B1), and the banner — shown only when gear is empty (#T1) — reworded from "Add your gear" to a gentle "Personalize your picks →". |
| 9 | #T4 | P2 | **"M" avatar** — wrong initial + dead tap. Derive the initial (or drop until a profile exists); if it stays, make it lead to Bag/settings. | ✅ no name to derive (anon auth), so dropped the mock "M" for a neutral person glyph; the circle is now a real tap-target → Bag (gear/settings), with a11y label + ripple. |
| 10 | #D2 | P2 | **Light chart under-explained** — one plain-language line ("taller = better light; you're here →") + make the "now" bar unmistakable. | ✅ added caption "Taller = better light. The gold-marked bar is right now."; "now" bar now pops (gold outline + stays full-opacity) with a gold ▾ caret above it (single glyph — 33 bars are too narrow for "NOW" text). |
| 11 | #P3 | P2 | **"fits your 35mm" repeats every Plan card** — show the fit label only when it *differs* from the obvious (e.g. "grab a tele"). | ✅ new `fitGapLabel` returns null when the kit covers the genre; Plan cards now show the note only on a real gap ("a stretch for your kit"), so it earns attention. +2 tests. |
| 12 | #B3 | P2 | **Surface "YOUR KIT SHOOTS" more** — it's the payoff of the Bag screen; give it prominence + consider echoing on Today ("your kit's built for street tonight"). | ✅ Bag: the section is now an elevated gold payoff panel (not a below-fold afterthought), relabeled **"YOUR KIT'S IDEAL FOR"**. Today: a quiet echo under the title — "Your kit's ideal for street, portraits & architecture." (humanized, up to 3 genres from the live kit). Framing: ideal-for, never a limit (per Desha's philosophy). |
| 13 | #T5 | P2 | **Today→Detail black flash on web** — verify on Pixel; if present, cross-fade from the current screen instead of fading up from black; confirm hero image loaded before animating. | ✅ (code) the #N1 overlay already cross-fades over Today (opacity 0 reveals it, not black); now also hold the entrance until the hero image paints (`onLoadEnd` + 250ms fallback). ⚑ final black-flash confirmation still needs the Pixel. |
| 14 | — | P2 | **No max-width (web stretch)** — cross-cutting refinement from E7's old P2 bucket; cap content width so the web preview doesn't stretch edge-to-edge. | ✅ content capped to a 440px phone-scale column, centered; pager uses the same capped width so swipe/scrollTo stay consistent. No-op on a real phone (window already narrower). |
| 15 | — | — | **On-device pass** (ties **Q6**) — mostly needs the hand: notification appearance, touch targets/thumb reach, safe areas (sticky CTA vs gesture bar), **haptics** (code-able via `expo-haptics`), contrast in real light, in-context permission timing. | ✅ (haptics only) added `expo-haptics` + `lib/haptics.ts` (native-only, fire-and-forget); wired tab switches + gear toggles = light tick, "I'm going" = medium bump. ⚑ STILL NEEDS THE PIXEL: notification look, thumb reach/touch targets, safe-area insets (sticky CTA vs gesture bar — no `react-native-safe-area-context` yet), contrast in real light, in-context permission timing. |

**Done (this workstream):**
- ✅ **#N1 (UX-N1) — Swipe navigation.** Today/Plan/Bag are a horizontal paging `ScrollView` in `App.tsx`; swipe + the bottom bar both drive it, kept in sync via `onScroll` (not `onMomentumScrollEnd`, which doesn't fire on web). Detail + Lock ride as absolute overlays so the pager keeps its scroll position. Replaced the old `screen`/`detailFrom` state with `tab` + `detailOpen`/`showLock`. Verified on web (user confirmed swipe + tab-sync); on-device pending an `eas build`.
- ✅ **#N2 (UX-N2) — Bottom-tab icons.** Heavier strokes (2), size 24, brighter inactive, gold "pill" behind the active tab, press ripple. (`BottomNav.tsx`)
- ✅ **#N3 (UX-N3) — Button press feedback.** All controls now have a pressed state (opacity/scale) + `android_ripple`: Today hero, Plan card+CTA, Bag Continue/Skip/camera + lens/camera/style chips + "pick your style" toggle, check-in buttons, `SpotRow` rows, `SpotDetail` back/heart circles + sticky CTA.
- ✅ **#N4 (UX-N4) — Overscroll bounce.** `alwaysBounceVertical` + `overScrollMode="always"` on Today/Plan/Bag/Detail scroll views.
- NOTE: these are JS changes — live on web immediately, but need an `eas build` to appear on the installed Pixel APK.

**Loop guidance:** decision-heavy rows (#D1 persist-vs-remove, #P2 hierarchy, #B2 first-run model) — make the flagged default call, note it in the commit, keep moving; don't block. Rows needing the physical phone (#T5 verify, most of the on-device pass) — do the code-able part, flag the rest for Desha's hands.

---

## E8 · Conditions & the shoot brief  ▶ (additive brief SHIPPED — from the P3 weather landing)
**ADR: `docs/engineering/SHOOT_BRIEF.html`** (Design — build next). Now that weather is a live signal, turn the nudge from a go/quiet **score** into a **brief**: given tonight's light × cloud × rain plus the user's kit — what's shootable, whether the gear's ready, and the shots to chase. Directly fixes the P3 side-effect where a 96% overcast evening drops the top score under the go bar and the "best near you" list (gated on the same bar) vanishes — overcast should mean "shoot flat-light street, this way," not a dead screen. Decisions (defaults called in the ADR): **D1** additive brief first vs. conditions re-rank (**leaning additive**, re-rank belongs in the one Events scorer — the load-bearing open call); **D2** rules-first (LLM later); **D3** rain **warn-first**, structured `weatherSealed` later; **D4** tunable f-stop readiness thresholds. Honesty: inferred-not-metered, ideal-for-never-a-limit.
- ✅ **CB1 — Conditions read.** Extended the Open-Meteo fetch to `cloud_cover,precipitation_probability,weather_code` (`weather.ts` → `Forecast` with `cloudAt`/`rainAt`/`wetAt`, renamed from `CloudCover`); `lib/conditions.ts` = `Conditions` type (phase × cloud × rain × wet × dark) + `conditionsFor(date, lat, lon, forecast?)`. `light.ts` exports `lightPhaseAt`. Null forecast → clear/dry, phase-only.
- ✅ **CB2 — Gear-readiness.** `lib/shootBrief.ts`: `fastestAperture(kit)` + `kitReadiness(kit, conditions)` over `maxAperture` → set (≤f/2) / workable (≤2.8) / borderline (≤4) / slow (>4) at night, tripod-nudge at blue, null in daylight. Every line ideal-for framed. No new data.
- ✅ **CB3 — Shot-type rules.** `shotsFor(conditions)` → 1–3 prompts; wet wins, then phase × cloud (overcast flat → even-light street; clear flat → hard-shadow; night → neon; blue → long-exposure).
- ✅ **CB4 — Surface it (additive).** `components/ShootBriefCard.tsx` under the hero (readiness dot + line, clay rain warning, shot list); `App.tsx` composes `shootBrief(shootAt, hero, kit, cloud)` read at the **evening** shoot moment (`max(now, golden-evening start)` — not the flat spot's morning `window.start`). Fills the gap a quiet-night empty "best near you" leaves. Verified in-browser (overcast/drizzle evening → seal warning + wet-street shots). 119 tests (+18). **Known simplification:** forecast is the city-proxy (`spots[0]`), so rain can read a neighboring grid cell's drizzle vs. the hero's own cell — city-scale honest, matches the weather layer's existing model.
- ◽ **CB5 — `weatherSealed` in the catalog** → personalize the rain warning. *(D3 v2, later)*
- ◽ **CB6 — Conditions re-rank** → gray nights promote flat-light spots, inside the one Events scorer. *(D1, later, if earned)*
- 🎯 **CB7 — Light quality is genre-dependent.** The shoot-brief work exposed that the nudge scorer's `BASE_QUALITY` (`golden 1.0 > blue 0.85 > flat 0.55`) carries a **landscape bias** — flagged in the Product Brief ("golden hour is a landscape obsession; don't down-rank my afternoon") and incoherent with the brief's own "overcast → even-light street" stance (scorer demotes flat-light street; brief celebrates it). **Researched** (deep-research: 22 sources, 25 claims adversarially verified, 0 refuted → `docs/engineering/LIGHT_GENRE_RESEARCH.md`): light isn't good/bad, it's creative modes; landscape is highly light-dependent, street is light-flexible (but *low, not zero* — color/light-forward street still times light, per Webb/Fan Ho). **ADR `docs/engineering/LIGHT_QUALITY_GENRE.html`** (Proposed · principle agreed, weights to calibrate): replace the global weight with a **per-genre light-sensitivity** (Street 0.15 … Landscape 0.90) + `phaseFit` map; `phaseScore = 1 − sensitivity·(1 − phaseFit[genre][phase])`. Chosen over leave-as-is / global-reweight. ✅ **SHIPPED** in `lib/nudge.ts`: `LIGHT_SENSITIVITY` per genre (Street 0.15 … Landscape 0.90) + shared `REF_FIT`; `phaseScore(genre, windowType) = 1 − sensitivity·(1 − REF_FIT[windowType])` replaces `BASE_QUALITY[windowType]`. A flat-light street spot is now barely penalized (~0.93 vs 0.55 before); a flat-light landscape spot still is. 6 tests (125 total), type-clean. `windowType` retained as the phase key (open Q resolved). **Deferred to calibration:** per-genre `phaseFit` inversion (Nature/Details preferring overcast); the exact weights; the color-street exception.

---

## E9 · Phase-honest surfaces  ▶ (PH1–5 SHIPPED Jul 16 · PH6 shipped+pulled · PH7 code Jul 17 — the CB7 follow-through)
**ADR: `docs/engineering/PHASE_HONEST_LIGHT.html`** (Accepted). CB7 made the *scorer* genre-honest; this epic makes the *surfaces* the photographer reads — the Today hero and the spot detail — **phase-honest**. Core principle (new voice rule, → `VOICE.md`): **light is always the light you have now, never a window you missed.** Kills the 9:30pm "golden hour hits 8:05" bug, un-blanks the quiet night, and re-orders the detail page around "what to shoot now" with light demoted to a slim strip. Design decisions **all locked** (thinking-through session, 2026-07-16): light placement = **Option A compact strip** (wired A/B/C in `detail-light-wires`); night module = **line + morning peek**; the "why" prose stays; flat light gets first-class positive framing. Mostly reuse — `shootBrief`/`conditionsFor`/`hourlyLight`/`goldenCountdown`/`kitReadiness` all exist. Build in order (bug first, then the shared engine reused across surfaces).

**✅ SHIPPED PH1–PH5 (2026-07-16).** New shared engine `lib/light.ts`: `lightRead()` (the one phase-honest copy source — chip/hero/strip strings), `forwardLight()` / `morningPeek()` / `lightStripModel()` (the forward sparkline + 3 states). New `components/LightStrip.tsx` (Option A). `ShootBriefCard` gained `title`/`trailing` props. `nudge.ts` gained `goodInTheDark()` (+`nightScore`). Hero (`InspirationHero`) takes a `LightRead`; `SpotDetail` takes the kit + leads with "What to shoot now" over the demoted strip; `App.tsx` wires `heroLight`, the "Good in the dark" shelf, and passes the kit to detail. **Typecheck clean; 136 tests pass (+11: `lightRead` night/flat/golden/blue reads incl. the 9:30pm regression, `forwardLight`/`morningPeek`, `goodInTheDark`).** **Verified live in-browser at a real 22:50 Atlanta night:** hero reads "The city's lit and yours" + ☾ Night chip (no stale golden); Today brief shows night shots; "Good in the dark" lists Olympic Rings / State Farm Arena / Ponce roof / Podium Plaza; detail shows "WHAT TO SHOOT NOW · 10:56pm · night — yours" over a slim "☾ LIGHT · Night — yours · next golden 6:39a" strip with the dawn morning-peek sparkline. **Bonus fix (found while verifying): the Today ShootBriefCard's `shootAt = max(now, goldenEvening.start)` previewed *tomorrow's* golden late at night** (getLightWindows rolls to the next solar day after midnight UTC) — now `shootAt = now` unless it's still flat daylight with golden ahead, so a 10pm check gets tonight's real conditions. Also removed the now-orphaned `components/LightChart.tsx` (the old full-width chart SpotDetail no longer uses; `hourlyLight` stays, still tested/reusable). NOT committed yet — working-tree changes for review.
- ✅ **PH1 — Hero goes phase-honest (🐛 the 9:30pm bug).** `InspirationHero` lede + chip read the *current* phase (`conditionsFor(now)`, like the ShootBriefCard already does at `App.tsx:169`) instead of always printing the evening golden window (`InspirationHero.tsx:53,60` → `goldenWindowLabel`). Past golden → blue/night copy; the ☀ chip shows the live phase, not a stale time. *AC: at a time after the evening golden window, the hero never names a passed golden time as tonight's plan; copy matches the phase the brief below it shows.*
- ✅ **PH2 — "Never say not now" light vocabulary.** The positive/forward copy rule across every place light is named (hero, detail, `nudgeCopy.ts` push, light-chart caption). Includes the **flat-light** framing (clear → graphic/hard-shadow; overcast → soft/even) and the forward countdown that rides *alongside* the current light, never demoting it. New micro-rule added to `VOICE.md`. *AC: no surface says "golden gone / blue closed / not now / harsh light"; flat daytime reads as a real option, not a wait-for-golden.*
- ✅ **PH3 — "What to shoot now" on the detail page.** Call `shootBrief(now, thisSpot, kit, cloud)` on `SpotDetail` and make it the body headline (block 3, above the demoted light + below the kept "why"). Reuses the Today engine, fed *now* + this spot. *AC: opening any spot shows time-aware, kit-aware shot guidance for this moment; "why" prose retained above it.*
- ✅ **PH4 — Demote the light chart → forward strip (Option A).** `SpotDetail` "When the light works" full-width card → a slim strip: one phase label + mini sparkline. `hourlyLight`/`LightChart` gain a **forward window** (`now → now+6h`) and the **three states** (light-ahead + countdown / in-window / full-night line + morning peek). *AC: the light module is no longer the first/full-width block; at night it's a line + tomorrow's-golden peek, not a flat-dark chart.*
- ✅ **PH5 — Quiet night → "Good in the dark."** When `bestNearYou` is empty, swap in a labeled shelf of evergreen spots live in the current phase, gated by `kitReadiness` (the user's motion-blur-on-a-bridge idea). A *separate* shelf, not padding the ranked list — the quality gate stays intact. *AC: an after-dark quiet night surfaces night-appropriate spots the kit can handle instead of hiding the section.*
- ◽ **PH6 — Street View link on detail** (shipped 2026-07-17, **HIDDEN same day**). A labeled **"See it on Street View"** row (its own affordance — *not* the title) deep-linking off `spot.lat/lon` via the **Maps URLs API** pano link (`?api=1&map_action=pano&viewpoint={lat},{lon}`). **⏸️ PULLED (2026-07-17) — pano deep-links are unreliable.** `map_action=pano` snaps to the *nearest* panorama to the point, which for a rooftop or interior spot lands in the wrong place — e.g. **Ponce City Market Roof resolved to the bookstore inside, not the roof.** That's inherent to deep-linking a coordinate (no way to say "the rooftop pano" without a curated pano ID), not a quick fix, so we **removed the feature** (row + `streetViewUrl` helper + tests) rather than ship something that misleads. Back to 136 tests, typecheck clean. **If revisited:** either (a) a plain Maps *location* link (`map_action=map` / `search?query=lat,lon`) — reliable, "see it on the map," but not the street-level view we wanted; or (b) a **curated per-spot `streetViewPanoId`** in the content pipeline (accurate, but manual per spot). **DECISION (2026-07-16): deep-link out, stay as-is.** *In-app* Street View was explored and deferred: the app has no WebView/maps native module, so (a) an inline **real-photo thumbnail** via the Street View **Static API** (`maps.googleapis.com/maps/api/streetview?...`) — just an `<Image>`, no rebuild, but a *paid* API (monthly free allowance; cache per spot); or (b) an **interactive in-app pano** via the Maps **Embed API** in `react-native-webview` — needs a **new EAS dev build** + a Maps API key. Both judged not worth the cost/rebuild for a nice-to-have now; the deep-link solves the core "see the real place" need. Revisit the static thumbnail (best fit — grounds the art without interactivity) if we're doing an EAS build anyway. *(logged as `later` — independent of the phase-honest core; do alongside PH3/PH4 or after.)*
- ✅ **PH7 — Phase-honest push (code shipped + DEPLOYED 2026-07-17).** *(The old "deploy pending" note was stale — verified 2026-07-22 by downloading the live function: it was byte-identical to the PH7 commit, deployed 12:18pm the same day it was committed.)* Brought the nightly Edge Function (`supabase/functions/nudge/index.ts`, the Deno mirror of the brain) in step with the app: (1) **CB7 scoring parity** — replaced the old global `BASE_QUALITY` (golden 1.0 > flat 0.55, a landscape bias) with the genre-dependent `phaseScore` (`LIGHT_SENSITIVITY` × `REF_FIT`), so the push picks the same spot the app shows (flat-light street no longer under-ranked: Street-flat 0.55 → **0.93**); (2) **phase-honest copy** — a **flat** pick no longer cites its window (which is *sunrise* — a morning time on an evening push!) and instead reads "good all evening"; golden/blue name the evening time only while it's still ahead, else "on now" (never a passed window); and the **no-go copy is positive** — dropped "the good light's already behind us" for "no big light tonight, but {spot} keeps" (VOICE: light is an asset, never a miss). **Verified the copy + scoring with a suncalc Node harness** at a simulated 6pm ATL send: golden → "Golden hour hits 8:11 PM", flat → no morning time, no "behind us". **Verified the copy + scoring with a suncalc Node harness** at a simulated 6pm ATL send: golden → "Golden hour hits 8:11 PM", flat → no morning time, no "behind us". ✅ **Deployed same day** (Supabase CLI now available in-env). **Weather parity DONE 2026-07-22** — `cloudFactor` ported in (see P3 follow-up above), so the push is no longer astronomical-only; app and push now agree on cloudy evenings.
